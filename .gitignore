# Build outputs and caches
/.xmake/
/build/
/compile_commands.json
/compile_flags.txt
*.elf
*.hex
*.bin
*.map
*.lst

# Local scratch and logs
/.cache/
/tmp/
/testlogs/

# Simulation files (not version controlled)
/simulation/

# Intentionally kept under version control:
# - /docs/**
# - /app/task/test/**
# Note:
# - workspace-root `om_preset.lua` and `.vscode/` are not owned by `new_robot_code`
